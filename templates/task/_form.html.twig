{{ form_start(form) }}
    <div class="container">
        {{ form_errors(form) }}

        <div class="form-row">
            <div class="form-group col-md-11">
                {{ form_row(form.name, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group col-md-1">
                {{ form_row(form.status, {'label': 'Status', 'attr': {'class': 'form-control'}}) }}
            </div>
        </div>

        <div style="display: none">{{ form_row(form.user, {'label': 'User:', 'attr': {'class': 'form-control'}}) }}</div>
    </div>

    <nav class="navbar navbar-expand-lg navbar-light bg-white">
        <div class="collapse navbar-collapse">
            <div class="navbar-nav mr-auto">
                <li class="nav-item"></li>
            </div>

            <ul class="navbar-nav">
                <li class="nav-item">
    {#                {{ include('task/_delete_form.html.twig') }}#}
                    <button class="btn btn-primary btn-w-200">{{ button_label|default('Save') }}</button>
                </li>
            </ul>
        </div>
    </nav>
{{ form_end(form) }}

{#{{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}
{#<button class="btn">{{ button_label|default('Save') }}</button>#}
{#{{ form_end(form) }}#}
